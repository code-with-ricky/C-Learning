#include<stdio.h>

int main() {
    // use printf() to output in the console
    printf("Hello World!");

    // by default, compiler does not go t next line
    // if want a new line after print then use \n
    printf("Line 1 printed\n");
    printf("Line 2 printed");
    return 0;
}